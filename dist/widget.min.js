(()=>{(()=>{let c=atob("aHR0cHM6Ly9teS1jaGF0LWJhY2tlbmQuZWR3YXJkYWdlbnQud29ya2Vycy5kZXY="),p=`
    <style>
      :host {
        --c-bg:        #0A0B12;
        --c-surface:   rgba(255, 255, 255, 0.05);
        --c-surface-2: rgba(255, 255, 255, 0.08);
        --c-line:      rgba(255, 255, 255, 0.10);
        --c-line-2:    rgba(255, 255, 255, 0.16);
        --c-ink:       #F5F5FA;
        --c-ink-2:     #C7C9D6;
        --c-muted:     #8A8DA0;
        --c-lime:      #A3E635;
        --c-cyan:      #22D3EE;
        --c-violet:    #8B5CF6;
        --c-red:       #F43F5E;
        --c-grad: linear-gradient(135deg, #A3E635 0%, #22D3EE 50%, #8B5CF6 100%);

        --ease: cubic-bezier(0.22, 1, 0.36, 1);

        position: fixed;
        right: 24px;
        bottom: 24px;
        z-index: 2147483000;
        font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        font-feature-settings: "ss01", "cv11";
        color: var(--c-ink);
        -webkit-font-smoothing: antialiased;
      }

      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

      button { font-family: inherit; cursor: pointer; border: none; background: none; color: inherit; }
      button:focus-visible { outline: 2px solid var(--c-cyan); outline-offset: 3px; border-radius: 8px; }

      /* ===== Launcher ===== */
      .launcher {
        width: 56px; height: 56px;
        border-radius: 50%;
        background: var(--c-grad);
        background-size: 200% 200%;
        background-position: 0% 50%;
        display: flex; align-items: center; justify-content: center;
        box-shadow: 0 8px 32px rgba(34, 211, 238, 0.35), 0 0 0 1px rgba(255, 255, 255, 0.1) inset;
        transition: transform 220ms var(--ease), box-shadow 220ms var(--ease), background-position 600ms var(--ease);
        position: relative;
      }
      .launcher::before {
        content: ''; position: absolute; inset: 0; border-radius: 50%;
        background: radial-gradient(circle at 30% 25%, rgba(255, 255, 255, 0.55), transparent 55%);
        pointer-events: none;
      }
      .launcher:hover { transform: translateY(-2px); box-shadow: 0 12px 40px rgba(34, 211, 238, 0.5); background-position: 100% 50%; }
      .launcher:active { transform: translateY(0); }
      .launcher svg { width: 24px; height: 24px; color: #06070A; position: relative; z-index: 1; transition: transform 220ms var(--ease), opacity 180ms var(--ease); }
      .launcher .icon-close { position: absolute; opacity: 0; transform: scale(0.6); }
      :host([open]) .launcher .icon-chat { opacity: 0; transform: scale(0.6); }
      :host([open]) .launcher .icon-close { opacity: 1; transform: scale(1); }

      .launcher .pulse {
        position: absolute; top: -2px; right: -2px;
        width: 12px; height: 12px; border-radius: 50%;
        background: var(--c-lime);
        box-shadow: 0 0 0 2px var(--c-bg), 0 0 12px var(--c-lime);
        animation: pulse 2.4s ease-in-out infinite;
      }
      :host([open]) .launcher .pulse { display: none; }
      @keyframes pulse { 0%, 100% { opacity: 0.6; transform: scale(1); } 50% { opacity: 1; transform: scale(1.15); } }

      /* ===== Panel ===== */
      .panel {
        position: absolute;
        right: 0; bottom: 72px;
        width: min(380px, calc(100vw - 32px));
        height: min(580px, calc(100vh - 120px));
        background: linear-gradient(180deg, rgba(15, 17, 26, 0.92), rgba(8, 9, 14, 0.96));
        border: 1px solid var(--c-line-2);
        border-radius: 20px;
        box-shadow: 0 40px 80px -20px rgba(0,0,0,0.7), 0 0 0 1px rgba(34, 211, 238, 0.08), 0 0 80px rgba(139, 92, 246, 0.18);
        backdrop-filter: blur(24px) saturate(160%);
        -webkit-backdrop-filter: blur(24px) saturate(160%);
        display: flex; flex-direction: column;
        overflow: hidden;
        opacity: 0; transform: translateY(12px) scale(0.98); pointer-events: none;
        transition: opacity 240ms var(--ease), transform 240ms var(--ease);
      }
      :host([open]) .panel { opacity: 1; transform: none; pointer-events: auto; }

      .panel::before {
        content: ''; position: absolute; inset: 0;
        background: radial-gradient(ellipse 80% 50% at 50% 0%, rgba(163, 230, 53, 0.08), transparent 60%);
        pointer-events: none;
      }

      /* ===== Header ===== */
      .head {
        display: flex; align-items: center; gap: 12px;
        padding: 16px 18px;
        border-bottom: 1px solid var(--c-line);
        position: relative;
        background: rgba(0, 0, 0, 0.18);
      }
      .head .mark {
        width: 32px; height: 32px; border-radius: 9px;
        background: var(--c-grad);
        position: relative;
        box-shadow: 0 0 16px rgba(34, 211, 238, 0.4);
      }
      .head .mark::after {
        content: ''; position: absolute; inset: 0; border-radius: 9px;
        background: radial-gradient(circle at 30% 25%, rgba(255, 255, 255, 0.6), transparent 55%);
      }
      .head .title { font-size: 14px; font-weight: 600; letter-spacing: -0.005em; }
      .head .sub { font-size: 12px; color: var(--c-muted); display: flex; align-items: center; gap: 6px; margin-top: 1px; }
      .head .dot { width: 6px; height: 6px; border-radius: 50%; background: var(--c-lime); box-shadow: 0 0 6px var(--c-lime); }
      .head .grow { flex: 1; }
      .head .icon-btn {
        width: 32px; height: 32px; border-radius: 8px;
        display: flex; align-items: center; justify-content: center;
        color: var(--c-ink-2);
        transition: background 180ms var(--ease), color 180ms var(--ease);
      }
      .head .icon-btn:hover { background: var(--c-surface); color: var(--c-ink); }
      .head .icon-btn svg { width: 16px; height: 16px; }

      /* ===== Messages ===== */
      .messages {
        flex: 1;
        overflow-y: auto;
        padding: 20px 18px;
        display: flex; flex-direction: column; gap: 14px;
        scroll-behavior: smooth;
      }
      .messages::-webkit-scrollbar { width: 6px; }
      .messages::-webkit-scrollbar-thumb { background: var(--c-line-2); border-radius: 3px; }
      .messages::-webkit-scrollbar-track { background: transparent; }

      .msg { display: flex; max-width: 85%; }
      .msg.user { align-self: flex-end; }
      .msg.bot  { align-self: flex-start; }

      .bubble {
        padding: 10px 14px;
        border-radius: 16px;
        font-size: 14px;
        line-height: 1.5;
        word-wrap: break-word;
        white-space: pre-wrap;
      }
      .msg.user .bubble {
        background: var(--c-grad);
        color: #06070A;
        font-weight: 500;
        border-bottom-right-radius: 6px;
        box-shadow: 0 4px 16px rgba(34, 211, 238, 0.22);
      }
      .msg.bot .bubble {
        background: var(--c-surface);
        border: 1px solid var(--c-line);
        color: var(--c-ink);
        border-bottom-left-radius: 6px;
      }
      .msg.error .bubble {
        background: rgba(244, 63, 94, 0.1);
        border: 1px solid rgba(244, 63, 94, 0.3);
        color: #FCA5A5;
      }

      .typing { display: inline-flex; gap: 4px; padding: 6px 0; }
      .typing span {
        width: 6px; height: 6px; border-radius: 50%;
        background: var(--c-muted);
        animation: bounce 1.2s ease-in-out infinite;
      }
      .typing span:nth-child(2) { animation-delay: 0.15s; }
      .typing span:nth-child(3) { animation-delay: 0.30s; }
      @keyframes bounce { 0%, 80%, 100% { transform: translateY(0); opacity: 0.4; } 40% { transform: translateY(-4px); opacity: 1; } }

      /* ===== Empty state ===== */
      .empty {
        flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center;
        text-align: center; padding: 24px;
        gap: 14px;
      }
      .empty .orb {
        width: 56px; height: 56px; border-radius: 50%;
        background: var(--c-grad);
        position: relative;
        box-shadow: 0 0 32px rgba(34, 211, 238, 0.4);
      }
      .empty .orb::after { content: ''; position: absolute; inset: 0; border-radius: 50%; background: radial-gradient(circle at 30% 25%, rgba(255, 255, 255, 0.6), transparent 55%); }
      .empty h3 { font-size: 17px; font-weight: 600; letter-spacing: -0.015em; }
      .empty p { font-size: 13px; color: var(--c-muted); max-width: 240px; line-height: 1.55; }
      .suggestions { display: flex; flex-direction: column; gap: 6px; margin-top: 8px; width: 100%; }
      .suggestion {
        background: var(--c-surface); border: 1px solid var(--c-line);
        border-radius: 10px; padding: 10px 12px;
        font-size: 13px; color: var(--c-ink-2); text-align: left;
        transition: background 180ms var(--ease), border-color 180ms var(--ease), color 180ms var(--ease), transform 180ms var(--ease);
      }
      .suggestion:hover { background: var(--c-surface-2); border-color: var(--c-line-2); color: var(--c-ink); transform: translateY(-1px); }

      /* ===== Composer ===== */
      .composer {
        padding: 12px 12px 14px;
        border-top: 1px solid var(--c-line);
        background: rgba(0, 0, 0, 0.22);
      }
      .composer-row {
        display: flex; align-items: flex-end; gap: 8px;
        background: var(--c-surface);
        border: 1px solid var(--c-line);
        border-radius: 14px;
        padding: 8px 8px 8px 14px;
        transition: border-color 180ms var(--ease), background 180ms var(--ease);
      }
      .composer-row:focus-within { border-color: rgba(163, 230, 53, 0.5); background: var(--c-surface-2); }
      textarea {
        flex: 1;
        background: transparent;
        border: none; outline: none;
        color: var(--c-ink);
        font-family: inherit;
        font-size: 14px;
        line-height: 1.5;
        resize: none;
        max-height: 120px;
        padding: 6px 0;
      }
      textarea::placeholder { color: var(--c-muted); }
      .send {
        width: 32px; height: 32px;
        border-radius: 10px;
        background: var(--c-grad);
        background-size: 200% 200%;
        background-position: 0% 50%;
        display: flex; align-items: center; justify-content: center;
        color: #06070A;
        flex-shrink: 0;
        transition: transform 180ms var(--ease), opacity 180ms var(--ease), background-position 400ms var(--ease);
      }
      .send:hover:not(:disabled) { background-position: 100% 50%; transform: translateY(-1px); }
      .send:disabled { opacity: 0.4; cursor: not-allowed; }
      .send svg { width: 16px; height: 16px; }
      .send.stop { background: var(--c-red); color: white; }

      .footer-note {
        margin-top: 8px;
        font-size: 11px;
        color: var(--c-muted);
        text-align: center;
      }

      @media (max-width: 480px) {
        :host { right: 12px; bottom: 12px; }
        .panel { width: calc(100vw - 24px); height: calc(100vh - 100px); right: 0; bottom: 68px; }
      }

      @media (prefers-reduced-motion: reduce) {
        *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
      }
    </style>

    <div class="panel" role="dialog" aria-label="Chat with Stratos assistant" aria-modal="false">
      <header class="head">
        <div class="mark" aria-hidden="true"></div>
        <div>
          <div class="title">Stratos Assistant</div>
          <div class="sub"><span class="dot"></span> Online \xB7 usually replies instantly</div>
        </div>
        <div class="grow"></div>
        <button class="icon-btn" type="button" data-action="reset" aria-label="Clear conversation" title="Clear conversation">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/></svg>
        </button>
        <button class="icon-btn" type="button" data-action="close" aria-label="Close chat">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
        </button>
      </header>

      <div class="messages" role="log" aria-live="polite" aria-atomic="false"></div>

      <div class="composer">
        <div class="composer-row">
          <textarea rows="1" placeholder="Ask anything about Stratos\u2026" aria-label="Message"></textarea>
          <button class="send" type="button" data-action="send" aria-label="Send message" disabled>
            <svg class="icon-send" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
            <svg class="icon-stop" viewBox="0 0 24 24" fill="currentColor" style="display:none;"><rect x="6" y="6" width="12" height="12" rx="2"/></svg>
          </button>
        </div>
        <div class="footer-note">Powered by Stratos \xB7 Press <kbd>Enter</kbd> to send</div>
      </div>
    </div>

    <button class="launcher" type="button" data-action="toggle" aria-label="Open chat">
      <span class="pulse" aria-hidden="true"></span>
      <svg class="icon-chat" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M21 15a4 4 0 0 1-4 4H7l-4 4V5a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2z"/>
      </svg>
      <svg class="icon-close" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M18 6L6 18M6 6l12 12"/>
      </svg>
    </button>
  `,h=["What chains does Stratos support?","Is my seed phrase safe with you?","How does cross-chain swap work?"];class g extends HTMLElement{constructor(){super(),this.attachShadow({mode:"open"}),this.shadowRoot.innerHTML=p,this.endpoint=this.getAttribute("endpoint")||c,this.messages=[],this.streaming=!1,this.abortCtrl=null,this.$panel=this.shadowRoot.querySelector(".panel"),this.$messages=this.shadowRoot.querySelector(".messages"),this.$textarea=this.shadowRoot.querySelector("textarea"),this.$send=this.shadowRoot.querySelector(".send"),this.$iconSend=this.shadowRoot.querySelector(".icon-send"),this.$iconStop=this.shadowRoot.querySelector(".icon-stop")}connectedCallback(){this.shadowRoot.addEventListener("click",e=>{let t=e.target.closest("[data-action]")?.dataset.action;t==="toggle"?this.toggle():t==="close"?this.close():t==="send"?this.streaming?this.stop():this.send():t==="reset"?this.reset():e.target.classList.contains("suggestion")&&(this.$textarea.value=e.target.textContent,this.send())}),this.$textarea.addEventListener("input",()=>{this.$textarea.style.height="auto",this.$textarea.style.height=Math.min(this.$textarea.scrollHeight,120)+"px",this.$send.disabled=this.streaming?!1:!this.$textarea.value.trim()}),this.$textarea.addEventListener("keydown",e=>{e.key==="Enter"&&!e.shiftKey&&(e.preventDefault(),!this.streaming&&this.$textarea.value.trim()&&this.send())}),document.addEventListener("keydown",e=>{e.key==="Escape"&&this.hasAttribute("open")&&this.close()}),this.renderEmpty()}toggle(){this.hasAttribute("open")?this.close():this.open()}open(){this.setAttribute("open",""),requestAnimationFrame(()=>this.$textarea.focus())}close(){this.removeAttribute("open")}reset(){this.streaming&&this.stop(),this.messages=[],this.renderEmpty()}renderEmpty(){this.$messages.innerHTML="";let e=document.createElement("div");e.className="empty",e.innerHTML=`
        <div class="orb" aria-hidden="true"></div>
        <h3>Hi there</h3>
        <p>Ask me anything about Stratos \u2014 chains, security, swaps, or how to get started.</p>
        <div class="suggestions"></div>
      `;let t=e.querySelector(".suggestions");h.forEach(a=>{let s=document.createElement("button");s.type="button",s.className="suggestion",s.textContent=a,t.appendChild(s)}),this.$messages.appendChild(e)}appendMessage(e,t=""){let a=this.$messages.querySelector(".empty");a&&a.remove();let s=document.createElement("div");s.className=`msg ${e}`;let r=document.createElement("div");return r.className="bubble",r.textContent=t,s.appendChild(r),this.$messages.appendChild(s),this.scrollToBottom(),r}appendTyping(){let e=document.createElement("div");e.className="msg bot";let t=document.createElement("div");return t.className="bubble",t.innerHTML='<span class="typing"><span></span><span></span><span></span></span>',e.appendChild(t),this.$messages.appendChild(e),this.scrollToBottom(),t}scrollToBottom(){this.$messages.scrollTop=this.$messages.scrollHeight}setStreaming(e){this.streaming=e,this.$iconSend.style.display=e?"none":"",this.$iconStop.style.display=e?"":"none",this.$send.classList.toggle("stop",e),this.$send.setAttribute("aria-label",e?"Stop generating":"Send message"),this.$send.disabled=e?!1:!this.$textarea.value.trim()}stop(){this.abortCtrl&&this.abortCtrl.abort()}async send(){let e=this.$textarea.value.trim();if(!e||this.streaming)return;this.appendMessage("user",e),this.messages.push({role:"user",content:e}),this.$textarea.value="",this.$textarea.style.height="auto";let t=this.appendTyping();this.setStreaming(!0),this.abortCtrl=new AbortController;try{let a=await fetch(this.endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages:this.messages}),signal:this.abortCtrl.signal});if(!a.ok)throw new Error(`Server returned ${a.status}`);if(!a.body)throw new Error("No response stream");let s=!0,r="",u=a.body.getReader(),b=new TextDecoder,o="";for(;;){let{value:m,done:x}=await u.read();if(x)break;o+=b.decode(m,{stream:!0});let i=o.split(`
`);o=i.pop()||"";for(let f of i){let n=f.trim();if(!n.startsWith("data:"))continue;let l=n.slice(5).trim();if(l!=="[DONE]")try{let d=JSON.parse(l).choices?.[0]?.delta?.content;d&&(s&&(t.textContent="",s=!1),r+=d,t.textContent=r,this.scrollToBottom())}catch{}}}r?this.messages.push({role:"assistant",content:r}):(t.parentElement.classList.add("error"),t.textContent="No response received. Try again.")}catch(a){a.name==="AbortError"?(t.textContent.trim()===""||t.querySelector(".typing"))&&t.parentElement.remove():(t.parentElement.classList.add("error"),t.textContent=`Couldn't reach the server. ${a.message}`)}finally{this.setStreaming(!1),this.abortCtrl=null,this.$textarea.focus()}}}customElements.get("stratos-chat")||customElements.define("stratos-chat",g)})();})();
